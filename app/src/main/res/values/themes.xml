<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MyApplication" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.MyApplication" parent="Base.Theme.MyApplication" />
    <style name="Theme.DadClicker" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">?attr/colorSurface</item>
        <item name = "materialButtonStyle">@style/Widget.DadClicker.CenterButton</item>
    </style>
    <style name="Widget.DadClicker.CenterButton" parent="Widget.Material3.Button.IconButton.Filled.Tonal">
        <item name="cornerRadius">28dp</item>
        <item name="minHeight">64dp</item>
        <item name="iconPadding">12dp</item>
        <item name="textAllCaps">false</item>
        <item name="android:textSize">18sp</item>
        <item name="paddingStart">22dp</item>
        <item name="paddingEnd">22dp</item>
    </style>
</resources>